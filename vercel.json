{
  "version": 2,
  "builds": [
    { "src": "api/*.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/gerar.js" },
    { "src": "/(.*)", "dest": "/public/$1" }
  ]
}
